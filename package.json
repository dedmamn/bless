{
  "name": "bless",
  "version": "1.0.0",
  "description": "",
  "main": "index.md",
  "scripts": {
    "build": "eleventy",
    "deploy": "npm run build && git add . && git commit -m 'deploy' && git push origin main"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
